version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps --include=dev
    build:
      commands:
        - ./node_modules/.bin/vite build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*